const add = (a, b) => {
	//Plan of action
	// 1. Save length of both strings
	// 2. Compare both lengths and add zeros with IFELSE
	// 2.1 If a is shorter than b, pad zeros to a with String.prototype.padStart()
	// 2.2 If b is shorter than a, pad zeros to b with String.prototype.padStart()
	// 3. Sum both strings up with magic (FORLOOP?), store it in result
	// 4. return result
}